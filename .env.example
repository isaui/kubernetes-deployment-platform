# Database settings
DATABASE_URL=postgresql://postgres:password@localhost:5432/pendeploy?schema=public

# JWT Authentication
JWT_SECRET=your_secure_jwt_secret_key_change_this_in_production

# Server settings
PORT=8080

# CORS settings
CORS_ALLOWED=http://localhost:5173

# Kubernetes configuration
# kubectl proxy address (run 'kubectl proxy' before starting the application)
K8S_PROXY_URL=http://localhost:8001

SERVER_IP=127.0.0.1